{
  "name": "@oneword/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist",
    "typecheck": "wrangler types && tsc --noEmit --pretty false",
    "test": "vitest run",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.1.13",
    "@types/node": "^26.6.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.0",
    "wrangler": "^4.135.0"
  }
}
